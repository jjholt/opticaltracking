[package]
name = "input"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1.3.1"
serde = { version = "1.0.217", features = ["derive"] }

[features]
defaults = ["polaris"]
polaris = []
certus = []
