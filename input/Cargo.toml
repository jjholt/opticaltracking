[package]
name = "input"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
fast-float = "0.2.0"
lexical = "7.0.5"
serde = { version = "1.0.228", features = ["derive"] }
