[package]
name = "jcs"
version = "0.1.0"
edition = "2024"

[dependencies]
attitude_determination = { path = "../attitude_determination" }
input = { path = "../input" }
[features]
default = ["knee"]
knee = []
hip = []
